apiVersion: v1
kind: Service
metadata:
  name: mytesthttpservice
  namespace: demo
spec:
  selector:
    app: mytesthttpserver
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

---
apiVersion: v1
kind: Pod
metadata:
  name: mytesthttpserver
  namespace: demo
spec:
  containers:
    - name: myhttpserver
      image: # plug here 
  restartPolicy: always
