# Postgres on RHEL8 Ansible Playbook

## Run the playbook locally

### Step 1: Install the modules
` ansible-galaxy collection install -r collections/requirements.yml `

### Step 2: Run the playbook
` ansible-playbook setup.yaml `
